// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://davefromgrogtag.github.io/',
    base: "/new-pangaea-print-site/"
});
